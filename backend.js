function backend() {
	function checkAccount(username) {
		//1) get all portals? (prototypes?)
		//2) test for any of these, whether there is an account
		//3) put all accounts in a json
		//4) return that json
	}
}